export const metadata = {
  title: "Login | QuickLearn Systems",
  description: "Sign in to your QuickLearn Systems account.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
